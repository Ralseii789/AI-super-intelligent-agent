spring:
  application:
    name: ai-agent
    ai:
      dashscope:
        api-key: ${SPRING_AI_DASHSCOPE_API_KEY}
        chat:
          options:
            model: qwen3-max
      mcp:
        client:
          sse:
            connections:
              shen-image-search-mcp-server:
                url: http://localhost:8127
#          stdio:
#            servers-configuration: classpath:mcp-servers.json
#      vectorstore:
#        pgvector:
#          index-type: HNSW
#          distance-type: COSINE_DISTANCE
#          dimensions: 1536
#          max-document-batch-size: 10000 # Optional: Maximum number of documents per batch
#  datasource:
#    url: jdbc:postgresql://rm-cn-v8n4s7dv60001bgo.rwlb.rds.aliyuncs.com:5432/shen_ai_agent
#    username: my_user
#    password: ${PgVector_Password}

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: alpha
  api-docs:
    path: /v3/api-docs
  group-configs:
    - group: 'default'
      paths-to-match: '/**'
      packages-to-scan: com.sdj.aiagent.controller
# knife4j???????????????
knife4j:
  enable: true
  setting:
    language: zh_cn
server:
  port: 8512
  servlet:
    context-path: /api
search-api:
  api-key: ${SEARCH-API}